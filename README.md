# autofeedback-testing-tool
A tool to test different LLMs to programming tasks

This is a mono repository. Use npm commands in the root folder.

You can either run the applications separately, or serve it from the backend.

To server from backend:
1. `npm install`
2. `npm run build`
3. `npm start`